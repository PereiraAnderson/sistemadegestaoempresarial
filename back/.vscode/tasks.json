{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "buildSGE",
            "command": "dotnet",
            "type": "shell",
            "args": [
                "build",
                "${workspaceFolder}"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}